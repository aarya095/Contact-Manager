info:
  name: Get all contacts stored in the database
  type: http
  seq: 3

http:
  method: GET
  url: http://127.0.0.1:8000/contacts
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
