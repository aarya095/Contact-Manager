info:
  name: Create Contact
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/contacts
  body:
    type: json
    data: |-
      {
        "contact_name": "string",
        "contact_number": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
