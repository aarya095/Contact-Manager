info:
  name: Update contact entry
  type: http
  seq: 4

http:
  method: PUT
  url: http://127.0.0.1:8000/contacts/{contact_id}
  body:
    type: json
    data: |-
      {
        "contact_name": "string",
        "contact_number": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
